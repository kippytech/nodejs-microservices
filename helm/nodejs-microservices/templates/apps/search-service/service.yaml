apiVersion: v1
kind: Service

metadata:
  name: search-service
  #namespace: {{ include "nodejs-microservices.namespace" . }}

spec:
  selector:
    app: search-service

  ports:
    - port: 3000
      targetPort: 3000

  type: ClusterIP