apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  #namespace: {{ .Values.namespace }} #development

spec:
  selector:
    app: api-gateway

  ports:
    - port: 3000
      targetPort: 3000
      #nodePort: 30080

  type: ClusterIP  #NodePort