apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: nodejs-microservices-staging
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/kippytech/nodejs-microservices.git
    targetRevision: main
    path: helm/nodejs-microservices

    helm:
      valueFiles:
        - values.yaml
        - values-staging.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: staging

  syncPolicy:
    syncOptions:
      - CreateNamespace=true